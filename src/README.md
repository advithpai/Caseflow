# Caseflow (source folder)

This README is a brief pointer specific to the `src/` folder. See the project root README for full instructions: `../README.md`.

- Key areas inside `src/`:
  - `components/` — UI components and form controls.
  - `workers/` — `csv-parser.worker.js` for CSV parsing.
  - `lib/` — `firebase.js` and helpers.
  - `pages/` — route-level pages for the app.

To run or deploy, follow the instructions in the repository root README.
